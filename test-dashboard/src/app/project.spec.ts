import { Project } from './project';

describe('Project', () => {
  it('should create an instance', () => {
    expect(new Project()).toBeTruthy();
  });
});
describe('ProjectWrap', () => {
  it('should create an instance', () => {
    expect(new ProjectWrap()).toBeTruthy();
  });
});
